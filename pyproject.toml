[project]
name = "proxy-rules-tools"
version = "0.1.0"
description = "Synchronization and validation tools for hanjl7/Proxy-Rules"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0,<7",
]

[tool.uv]
package = false
